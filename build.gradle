// Root build.gradle — minimal for MultiLoader Template.
// All configuration is in each subproject's build.gradle.

subprojects {
    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
    }
}